# Escribí un programa que solicite el ingreso de un texto formado por una o más líneas. El ingreso finalizará cuando se lea una línea terminada con un '*'

# Calcular e informar:
# Las palabras que tienen al menos 3 vocales diferentes (ignorando diferencias de acentos y mayúsculas).
# La cantidad de oraciones en el texto (una oración finaliza siempre con un '.').
# El porcentaje de oraciones con más de cinco palabras.

import funciones

funciones.procesar_palabras()