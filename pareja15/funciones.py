def listaL(nombre : str) -> list:
    """Función que recibe un nombre y devuelve una lista con cada letra del nombre como un elemento de la lista.
     Args:
    nombre (str): Nombre a convertir en lista de letras."""
    return [letra for letra in nombre]     